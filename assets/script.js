// Assigned Variables
var charLower = "abcdefghijklmnopqrstuvwxyz";
var charUpper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
var charNum = "0123456789"
var charSpecial = "!@#$%^&*()_+-{}.[],/:'<?>;=";
var password = generatePassword();
var passwordText = document.querySelector("#password");
var generateBtn = document.querySelector("#generate");

// Write password to the #password input
function writePassword() {
  

  // Create password length between 8-128 characters

  var passwordLength = for (var i = 0; i > 128; i++ || var i = 0; i < 8; i++);
  alert("Password length must be between 8 and 128 characters");
    writePassword();
    return passwordLength
  }
 
  // Create character types to include (uppercase, lowercase, numeric, special characters)
  passwordText.value = password;

}
var passwordLength = prompt(
  "How many characters would you like your password to contain?"
  );
var charAlpha = confirm(
  "Click OK to confirm including letters."
  );
var charNum = confirm(
  "Click OK to confirm including numbers."
  );
var charSpecial = confirm(
  "Click OK to confirm including special characters."
  );
var charUpper  = confirm(
  "Click OK to confirm including uppercase letters."
  );
var charLower = confirm(
  "Click OK to confirm including lowercase letters."
  );
function generatePassword() {
  if 
  return "This will be replaced by my generated password.";
}

// Add event listener to generate button
generateBtn.addEventListener("click", writePassword);


// Tools
// Conditions (if, else statements)
// Use Math.random() to generate random numbers
// Arrays
// Loops (for loop to iterate through arrays)